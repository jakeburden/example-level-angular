{
  "name": "example-level-angular",
  "version": "1.0.0",
  "description": "super simple example of using angular.js with leveldb",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node index.js",
    "build": "browserify app/js/app.js > app/views/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jacob Burden",
  "license": "ISC",
  "dependencies": {
    "angular": "^1.3.16",
    "concat-stream": "^1.4.10",
    "ecstatic": "^0.8.0",
    "level": "^1.1.0",
    "routes": "^2.0.0"
  },
  "devDependencies": {
    "browserify": "^10.2.4"
  }
}
